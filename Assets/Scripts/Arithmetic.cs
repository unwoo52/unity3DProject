using Player;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;


namespace Atithmethic
{
    static public class Arithmetic
    {
        /*
        static public Vector3 GetMidMoniter()
        {
            //플레이어 모니터 상태 체크
            if (PlayerScript.PlayerInstance._state.isCurrentFp)
                return new Vector3(_components.fpCamera.pixelWidth / 2, _components.fpCamera.pixelHeight / 2);
            else
                return new Vector3(_components.tpCamera.pixelWidth / 2, _components.tpCamera.pixelHeight / 2);

        }*/
    }
}

